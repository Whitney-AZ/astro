# 验收记录

检查日期：2026-09-15。此记录不含行程、口令、摘要、会话密钥或截图。

## 已完成

| 检查       | 结果与边界                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------------- |
| 原件导入   | 私有输入数量符合要求；原 ID、非投影字段、日期与来源逐字段一致；Bokeh 投影与 WGS84 交叉核验通过                              |
| 自动化测试 | `japan:test` 16/16 通过：包括安全逻辑、私有真实数据、Vercel 输出结构及真实 Redis 测试                                       |
| 真实 Redis | 两个独立客户端并发限速计数、TTL、原子冲突、历史保留和会话撤销通过；使用本机 Redis 8.2.1，生产 REST 服务尚未接入             |
| 浏览器功能 | Playwright 桌面和手机共 10/10 通过；瓦片为 mock，使用真实本机 Redis 和独立合成行程                                          |
| 真实底图   | 单独的桌面、手机测试均通过；八个指定区域各检查一个 z15 视窗，所有视窗内瓦片在真实浏览器成功解码；首次登录默认详细底图为 z14 |
| 界面       | 检查了真实桌面与手机截图；修复中文标签挤成竖排的问题；手机地图与可滚动面板分开，补充点线键盘可访问名称                      |
| 构建       | `pnpm build` 通过；Astro 0 errors、0 warnings、2 个既有 hints；Pagefind 索引 211 页；最终允许网络的构建中 Bangumi 获取成功  |
| 格式       | `pnpm japan:lint` 和 `git diff --check` 通过；没有运行会重写整个博客的原 `pnpm lint`                                        |
| 公开产物   | 静态站、函数包、索引、Git 受跟踪及待加入文件通过私密标记和 Secret 扫描；私密资料在忽略目录                                  |
| 密钥初始化 | 通过隐藏输入生成本机受限文件；没有上传到部署平台，没有打印变量值                                                            |

私有证据在 `.private/japan/qa/`。mock 浏览器测试与真实瓦片测试是两条独立测试，不将 mock 成功计作真实网络成功。测试中无密码截图、公开 CI artifact、第三方访客分析或瓦片扫描。

## 尚未完成生产验收

正式 URL 的实际只读探测仍为：裸域名 `/japan` 307 到 `www`，`www /japan` 为 404。没有 Vercel 项目关联、部署凭证或已配置的私有 Redis REST 实例；没有执行生产部署、创建付费服务或改动 DNS。

生产尚须执行维护说明中的私有存储接入、五个运行时变量配置、私有数据上传、现有项目部署和裸域名跳转例外。完成后需要实测 Vercel 路由、可信 IP 头、原始部署域名拒绝访问、跨客户端／边缘节点缓存隔离以及现有博客路由。

本地新客户端无法读取另一已登录客户端访问过的数据，且响应带禁止公共缓存头；这不能替代真实 CDN 上的缓存隔离验收。当前交付状态是**代码、本地验证和部署准备完成，尚未上线**。
